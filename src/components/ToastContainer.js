import React from "react";
import { ToastContainer } from "react-toastify";

import "react-toastify/dist/ReactToastify.css";

const App = () => <ToastContainer hideProgressBar={true} />;

export default App;
